import React from 'react';
import { Typography } from '@mui/material';

//===============================================================

const FlightsPage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <Typography variant="h4">Uçuş Listeleme Sayfası</Typography>
      <Typography>Kişi 2'nin alanı. Filtrelenen uçuşlar burada listelenecek.</Typography>
    </div>
  );
};

export default FlightsPage;
